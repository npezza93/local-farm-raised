%li.mdc-list-item{ id: user.id }
  = user.email
  = link_to user_path(user, page: params[:page]),
      class: "mdc-list-item__end-detail material-icons", method: :put do
    = user.admin? ? "lock_open" : "lock_outline"
