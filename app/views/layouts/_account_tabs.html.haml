%nav.mdc-tab-bar.mt-2
  = link_to "Settings", edit_user_registration_path,
      class: "mdc-tab #{'mdc-tab--active' if active_tab == :settings}"
  = link_to "Address Book", addresses_path,
      class: "mdc-tab #{'mdc-tab--active' if active_tab == :addresses}"
  = link_to "Wallet", credit_cards_path,
      class: "mdc-tab #{'mdc-tab--active' if active_tab == :wallet}"
  = link_to "Orders", orders_path,
      class: "mdc-tab #{'mdc-tab--active' if active_tab == :orders}"
  - if current_user&.admin?
    = link_to "Users", users_path,
        class: "mdc-tab #{'mdc-tab--active' if active_tab == :users}"
  %span.mdc-tab-bar__indicator
